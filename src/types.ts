//defines the shape of a single book object
export type bookListProps = {
  title: string;
  category: string;
  description: string;
  price: number;
};

export type bookListArrayProps = bookListProps[];
